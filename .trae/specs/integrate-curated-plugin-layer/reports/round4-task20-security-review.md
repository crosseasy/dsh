# Task 20.1 Independent Security Review

| # | Category | Title | Severity | Confidence | Evidence (Source -> Sink) | Recommendation | Location |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | code_execution | Unannotated profile entries bypass observed preflight before dynamic import | HIGH | 0.98 | An untrusted `cordis.patch.yml` is added to the observed composition (`src/index.ts` L1479-L1499) -> entries without `config.curated` return no issue and never enter capability admission (`src/index.ts` L2952-L2958) -> preflight can return `accepted` with no issues -> normal CLI boot composes the same profile patch (`apps/cli/src/profile-boot.ts` L145-L173, L243-L251) -> Cordis imports the attacker-selected entry `name` (`vendor/loader/src/config/tree.ts` L144-L160). A profile can therefore preserve the exact curated bundle manifest while adding `- insert: [{ id: evil, name: ./evil.mjs }]`, pass observed preflight, and execute that module when launched. | Require every effective inserted row in an observed curated profile to be attributable to an approved catalog or installation-owned bundle; reject profile-owned or otherwise unattributed module specifiers before returning `accepted`. | [`packages/curated/curated-scripts/src/index.ts:[2952,2958]`](file:///Users/bytedance/opencode/agent/dsh/packages/curated/curated-scripts/src/index.ts#L2952-L2958) |
| 2 | code_execution | Observed artifact provenance is not bound to the installed bytes | HIGH | 0.94 | An untrusted standalone package supplies its own repository, commit, and digest sidecar (`src/index.ts` L583-L610), while a managed profile supplies both lockfiles and the installed package (`src/index.ts` L724-L789) -> validation compares attacker-copyable metadata, manifest fields, file existence, and entry IDs but never hashes or independently verifies the installed files (`src/index.ts` L960-L1139) -> `verify-lock` returns `observed: true` and `ok: true` when those comparisons match (`src/index.ts` L556-L570) -> the same package or patch is later imported by profile boot. An altered module or patch retaining expected metadata can therefore pass lock, preflight, and smoke admission before code execution. | Bind admission to independently acquired artifact bytes: verify registry SRI or a pinned Git archive/tree digest against the actual inspected package contents, and authenticate standalone acquisition records outside the artifact they attest. | [`packages/curated/curated-scripts/src/index.ts:[583,610]`](file:///Users/bytedance/opencode/agent/dsh/packages/curated/curated-scripts/src/index.ts#L583-L610)<br>[`packages/curated/curated-scripts/src/index.ts:[724,789]`](file:///Users/bytedance/opencode/agent/dsh/packages/curated/curated-scripts/src/index.ts#L724-L789) |
| 3 | sensitive_data_exposure | Malformed YAML diagnostics disclose credential suffixes | MEDIUM | 0.98 | An untrusted catalog, lockfile, workspace, or patch is parsed by `js-yaml`, whose exception includes a multiline source code frame (`src/index.ts` L2537-L2542) -> the text redactor replaces only the first non-whitespace token after a secret-shaped key (`src/index.ts` L3377-L3388) -> `formatThrown` writes the remainder to stdout (`src/index.ts` L3286-L3296). For example, a malformed YAML file containing `apiKey: correct horse battery staple` is rendered with only `correct` redacted, exposing `horse battery staple`; the policy loader's separate redactor returns the entire message unchanged unless the value matches a narrow token pattern (`curated-policy/src/index.ts` L940-L945, L1289-L1290). | Remove YAML code frames before reporting parser failures, or redact the complete scalar value and continuation for every secret-shaped key; use the same fail-closed diagnostic sanitizer in policy and command loaders. | [`packages/curated/curated-scripts/src/index.ts:[3377,3388]`](file:///Users/bytedance/opencode/agent/dsh/packages/curated/curated-scripts/src/index.ts#L3377-L3388)<br>[`packages/curated/curated-policy/src/index.ts:[1289,1290]`](file:///Users/bytedance/opencode/agent/dsh/packages/curated/curated-policy/src/index.ts#L1289-L1290) |
