#!/usr/bin/env node
/* v8 ignore file -- exercised through the installed package-manager shim. */
/** Published preflight executable. @module @deepseek-ai/dsh-curated-scripts/preflight */

import { runCuratedCommand } from './bin.ts'

await runCuratedCommand('preflight')
