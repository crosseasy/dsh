#!/usr/bin/env node
/* v8 ignore file -- exercised through the installed package-manager shim. */
/** Published verify-lock executable. @module @deepseek-ai/dsh-curated-scripts/verify-lock */

import { runCuratedCommand } from './bin.ts'

await runCuratedCommand('verify-lock')
