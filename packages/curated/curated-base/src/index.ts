/**
 * @deepseek-ai/dsh-curated-base — static bundle package for the curated
 * service layer. The package's substance is `cordis.patch.yml`, declared by
 * the `dsh.bundle.patch` manifest field and resolved by the profile composer;
 * this module carries no runtime API.
 * @module @deepseek-ai/dsh-curated-base
 */

export {}
