/**
 * @deepseek-ai/dsh-fusion — a pure profile patch layer for curated external
 * plugins over dsh-web-app. The package's substance is `cordis.patch.yml`,
 * declared by the `dsh.bundle.patch` manifest field; this module carries no
 * runtime API.
 * @module @deepseek-ai/dsh-fusion
 */

export {}
